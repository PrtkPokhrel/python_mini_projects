# default argument
"""
def shouldContinue(prompt, answer=False):
    print(f"prompt:{prompt}")
    print(f"answer:{answer}")


shouldContinue("Do you wish to continue")
shouldContinue("Do you wish to continue",True)
"""
# default argument allows a function to have a default set of parameters that even when the value is not provided to the
# parameter the function has the default value of specific parameters


# Keyword argument

# def shouldContinue(prompt, answer=False):